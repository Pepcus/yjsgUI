/* eslint-disable import/no-extraneous-dependencies */
import styled from 'styled-components';

import { Row } from 'pepcus-core';

const ImageWrapper = styled(Row)`
  max-width: 200px;
`;

export default ImageWrapper;
